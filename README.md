# CC98 AI+

CC98 论坛的浏览器插件（Chrome / Edge），两个功能：

- **AI 搜索** — 输入一个话题，自动拆关键词 → 搜帖 → 读楼层回复 → 过滤水帖 → 生成带来源引用的总结。全程在你自己电脑上跑，用你自己的 CC98 登录态和 AI API Key。
- **订阅提醒** — 订阅感兴趣的关键词，有新帖命中就通过钉钉或邮箱推送给你。这部分需要连后端。

只想用 AI 搜索的话，**跳过后端相关的全部内容**，配好第一节就能用。

---

# 一、给使用者

## 1. 安装插件

1. Chrome 地址栏输入 `chrome://extensions`
2. 右上角打开「**开发者模式**」
3. 点「**加载已解压的扩展程序**」，选中 `cc98-ai-plus` 这个文件夹（是文件夹本身，不是里面的 src）

装好后打开 `https://www.cc98.org` 并登录，右下角会出现 🤖 按钮。

> **看不到 🤖？** 装插件前就开着的页面不会自动注入，按 `F5` 刷新一下。

## 2. 配置 AI（必须，否则搜索用不了）

右键插件图标 → **选项**（或在插件面板里点 ⚙️ 设置 → 跳转设置页）。

### 填什么

你需要一个 AI 服务商的 API Key。选一家注册，把对应那行的三个值填进去：

| 服务商 | Base URL | 模型填 | 说明 |
|---|---|---|---|
| **DeepSeek** | `https://api.deepseek.com` | `deepseek-chat` | 便宜、效果好，**推荐** |
| **硅基流动** | `https://api.siliconflow.cn/v1` | `Qwen/Qwen2.5-14B-Instruct` | 注册送额度 |
| **智谱 AI** | `https://open.bigmodel.cn/api/paas/v4` | `glm-4-flash` | 该模型免费 |
| **阿里百炼** | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `qwen-plus` | 有免费额度 |

Provider 保持「**OpenAI 兼容**」——上面四家都走这个协议。只有用 Anthropic 官方 Key 时才切成 Anthropic。

拿 Key 的地方一般在服务商后台的「API 密钥」页面，生成一串 `sk-` 开头的字符串。

### 填完必须做两件事

1. 点「**测试连接**」，出现绿色「连接成功 ✓」
2. **滚到页面最底下点「保存设置」**

> 只测试不保存等于没配。测试连接读的是输入框，搜索时读的是已保存的值。

> **第一次用非默认服务商（DeepSeek / 智谱 / 阿里百炼）时，点「测试连接」或「保存」会弹一次浏览器授权框**，问你「允许访问 xxx 吗」，点「允许」即可。因为插件默认只放行了 CC98 和硅基流动，其他服务商按需授权，只弹这一次。

### Max Tokens 保持 0

`0` 表示不限制，由模型自己决定输出长度。

**别随便调小**。像 `deepseek-reasoner`、`deepseek-v4-pro` 这类推理模型会先输出一大段思维链，思维链和正文共用这个额度——设成 4096 的话，额度可能全被思维链吃光，正文一个字都出不来，搜索会在最后一步失败。

## 3. 用 AI 搜索

点右下角 🤖（或按 `Ctrl+Shift+K`）→「🔍 搜索」→ 输入话题 → 回车。

进度条会依次走过：拆分关键词 → 搜索 CC98 → 筛选主题 → 阅读回复 → 生成总结。

**一次搜索大约要 1～2 分钟**，因为 CC98 接口有 1.2 秒的限速，还要逐个读帖子的回复。进度条在动就是正常的，耐心等。

结果包含：用了哪些关键词、Markdown 格式的总结、以及带编号的来源帖子链接（可点开原帖）。

### 嫌慢或嫌费钱

设置页的「搜索预算」控制挖多深，默认是拆 4 个关键词 → 每词搜 20 条 → 选 10 个帖子 → 每帖读 30 楼。

- **想快**：把「选取主题数」降到 5、「每主题最多读回复」降到 15
- **想全**：反过来调大，但会更慢更费 token

### 过滤戾气 / 水帖（可选）

搜索会自动过滤掉带戾气、辱骂语气的回复。过滤强度在**设置页 → 内容过滤**里调：

- **启用过滤**：总开关
- **启用本地模型**：用本地 BGE 模型抓语气（关掉则只靠脏话词表，更快但抓不准）
- **过滤强度**：滑块越靠右越严（`0.35` 宽松 ~ `0.95` 严格），默认 `0.56`

## 4. 校外访问（WebVPN / RVPN）

**不用做任何设置，插件自动适配。**

- 走 **RVPN / ZJU-Connect** 直连 `https://www.cc98.org` → 正常工作
- 走 **WebVPN**（`webvpn.zju.edu.cn/https/...`）→ 插件自动把接口地址转换成 WebVPN 的编码路径，搜索结果里的帖子链接也会指向 WebVPN，点开就能看

技术细节见「四、实现说明」。

## 5. 订阅提醒

**这部分需要后端在运行**，没部署后端的话这三个 Tab 用不了。

### 登录

🤖 → ⚙️ 设置 → 填你的**浙大邮箱**（`@zju.edu.cn`）→ 发送验证码 → 收信 → 填 6 位码 → 验证登录。

### 添加订阅

→ 📋 订阅 → 填名称和关键词 → 添加。

**关键词怎么写，直接决定你能不能收到通知：**

匹配规则是——把**订阅名称 + 关键词**切成词，在新帖的**标题和正文**里找，**出现任意一个就提醒你**。

⚠️ **中文不会自动分词**，所以必须用空格把词分开：

| 写法 | 结果 |
|---|---|
| `实习 内推 招聘` | ✅ 出现任意一个词就提醒 |
| `我想找后端实习` | ❌ 整句被当成一个词，几乎不可能命中 |

词太宽泛（比如只写 `实习`）会收到很多不相关的；太具体又可能一条都收不到。建议 3～6 个词，从宽到窄慢慢调。

一个账号最多同时启用 **10 个**订阅，用不到的可以「暂停」而不必删除。

### 通知设置

→ ⚙️ 设置 → 选**通知间隔**，配**钉钉**或**邮箱**渠道。

**间隔是「攒一批发」的意思**：后端固定每 10 分钟扫一次新帖，命中的先攒着，到你选的间隔（比如 1 小时）再把这段时间攒下的合并成**一条**消息发给你。选 1 小时不代表 1 小时才扫，只是不想被一条条刷屏。

**钉钉**（推荐，自己就能搞定）：
1. 钉钉里建个群（只有自己也行）→ 群设置 → 智能群助手 → 添加机器人 → **自定义**
2. 安全设置勾「**加签**」，复制 Webhook 地址和 `SEC` 开头的密钥
3. 填进设置页 → 勾启用 → **保存** → **测试**

**邮箱**：填你要接收的邮箱地址即可，发信服务器由部署方统一配置。

### 通知列表

→ 🔔 通知。能看到命中的帖子、**为什么命中**（命中了哪些关键词）、以及发送状态。

状态「**未配置渠道**」不是错误，意思是匹配成功了但你还没配钉钉/邮箱，所以没往外发。

点「🔄 立即检查新帖」可以手动触发一次扫描，不用等定时任务。

---

# 二、给部署方（跑后端的人）

订阅提醒需要后端。只用 AI 搜索的话这一节可以整个跳过。

后端仓库：`cc98-ai-improvement-backend`

## 1. 启动

在 PowerShell 里，**一条一条执行**：

```powershell
cd C:\path\to\cc98-ai-improvement-backend
```

```powershell
python -m venv .venv
```

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

```powershell
.\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8000 --reload
```

看到 `Uvicorn running on http://127.0.0.1:8000` 就成功了。**这个窗口要一直开着**，关掉后端就停。停服务按 `Ctrl+C`。

验证：浏览器打开 `http://127.0.0.1:8000/api/v1/health`，返回 `{"status":"ok",...}` 即可。
接口文档在 `http://127.0.0.1:8000/docs`，能直接点着测。

## 2. 配置 .env

复制 `.env.example` 为 `.env` 再改。几个关键项：

### 发信账号（发验证码和邮件通知都靠它）

这是**整个产品配一次**的发信箱，相当于 `noreply@`。用户看不到也不需要配，他们只填自己的收件地址。

以 QQ 邮箱为例：

```
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USE_SSL=true
SMTP_USERNAME=你的QQ号@qq.com
SMTP_PASSWORD=16位授权码
SMTP_FROM=你的QQ号@qq.com
```

**授权码不是 QQ 登录密码**。去 `mail.qq.com` → 设置 → 账号与安全 → 开启「IMAP/SMTP服务」→ 短信验证 → 得到一串 16 位字母。它只能用来收发邮件，泄露了也动不了 QQ 账号，可以随时吊销。

### 登录验证码

```
AUTH_DEV_PRINT_CODE=false
```

- `false`（正式）：验证码真的寄到用户邮箱，界面走完整的输入验证码流程
- `true`（调试）：验证码直接回给前端，**点一下发送就自动登录**，不用收信

调试时开 `true` 很省事，但别忘了上线前改回 `false`。

### 定时扫描

```
ENABLE_SCHEDULER=true
SCAN_INTERVAL_MINUTES=10
```

- `true`：每 10 分钟自动扫一次（正式运行该开）
- `false`：不自动扫，只能在插件里手点「立即检查新帖」

### 帖子来源

```
WATCH_FORCE_MOCK_TOPICS=true
```

- `true`：用模拟帖子，**不需要 CC98 账号就能跑通全流程**，适合本地开发和演示
- `false`：抓真实新帖，需要填 `CC98_SERVICE_USERNAME` / `CC98_SERVICE_PASSWORD`（一个专供后端用的 CC98 账号），而且**后端所在机器要能访问 `api.cc98.org`**（校园网或挂 RVPN）

### 匹配方式

```
MATCHER_FORCE_RULES=true
```

- `true`：纯关键词匹配
- `false` + 填 `AI_API_KEY`：先关键词粗筛，命中的再让 LLM 复核一遍去掉误报

注意开了 AI 也**不会**理解用户的自然语言描述——关键词粗筛没命中会直接否决，压根轮不到 LLM。所以引导用户写好关键词比开 AI 重要。

> 改完 `.env` **必须手动重启后端**。`--reload` 只监听 `.py` 文件，配置文件的改动它不管。

## 3. 让插件连上

插件设置页最下面的「后端地址」默认是 `http://127.0.0.1:8000`。部署到服务器后改成实际地址。

## 4. 安全

`.env` 里有 SMTP 授权码等密钥，**确认它在 `.gitignore` 里**（后端仓库默认已包含），别提交到 GitHub。

---

# 三、常见问题

**右下角没有 🤖**
装插件前就开着的页面不会注入，`F5` 刷新。改过插件代码的话，要先在 `chrome://extensions` 点 🔄 重新加载，再刷新页面。

**「CC98 登录凭证无效或已过期」**
CC98 的登录态一小时过期。刷新 CC98 页面即可，页面会自动续期。

**「LLM 服务拒绝了这个 API Key」**
Key 和 Base URL 不配套。官方 Key 要配官方地址，中转站的 Key 要配中转站地址。展开「技术详情」能看到服务商返回的原话。

**「模型输出被 Max Tokens 截断」**
把 Max Tokens 改成 `0`（不限制），或换成非推理模型如 `deepseek-chat`。

**搜索很慢**
正常，CC98 接口限速 1.2 秒。想快就调小「选取主题数」和「每主题最多读回复」。

**订阅了但收不到通知**
八成是关键词写法问题，见「添加订阅」那节——中文必须用空格分开写。也确认一下后端在跑、订阅状态是「启用」而非「暂停」。

**遇到别的错误**
错误框里展开「技术详情」，那里有服务器返回的原始内容。插件的日志在 `F12` → Console，搜 `[CC98 AI+]`。

---

# 四、实现说明

## 目录结构

```
cc98-ai-plus/
├── manifest.json                    # MV3 配置
├── vendor/marked.esm.js             # Markdown 渲染，本地打包不走 CDN
└── src/
    ├── shared/constants.js          # 全局常量
    ├── background/service-worker.js # 网络代理 + 未读徽章轮询
    ├── content/
    │   ├── index.js                 # 内容脚本入口（动态 import 加载）
    │   ├── main.js                  # 初始化 UI + 快捷键
    │   ├── lib/                     # storage / net / errors / cc98-api / llm-client
    │   │                            # token-extractor / webvpn / auth / backend-api
    │   ├── pipeline/                # 搜索管线：keyword→search→filter→read→summarize
    │   ├── cleaner/                 # 内容过滤（规则层 + 本地 BGE 模型）
    │   └── ui/                      # 悬浮按钮 / 面板 / 进度条 / 各 Tab
    └── pages/
        ├── popup/                   # 工具栏弹窗
        └── options/                 # 设置页
```

## 安全边界

- **所有网络请求都经后台 Service Worker 转发**，绕过浏览器 CORS——CC98 和各家 LLM 接口都不放行浏览器跨域。
- **CC98 登录态**存在 `chrome.storage.session`（内存，关浏览器即清空），只发给 `api.cc98.org`。
- **LLM API Key** 存在 `chrome.storage.local`，只发给你自己填的 Base URL。
- **两者都不会给后端**。后端只做订阅扫描和通知，用它自己的 CC98 账号，用你的浙大邮箱作用户标识。
- 转发请求默认 **不携带 cookie**（`credentials: 'omit'`），避免把站点凭证泄露给 LLM 服务商；只有 WebVPN 请求例外，因为它靠 cookie 维持会话。
- 论坛内容会用 `<forum_content>` 标签包裹后再交给 LLM，降低提示词注入风险。

## WebVPN 适配

WebVPN（深信服方案）把主机名用 **AES-128-CFB** 加密进路径，key 和 IV 都是 `wrdvpnisthebest!`，最终路径为 `hex(IV) + hex(密文)`。

- 网页链接：直接复用当前地址里已经编码好的前缀
- 接口地址：现算 `api.cc98.org` 的编码路径（带缓存）

WebCrypto 不支持 CFB 模式，所以用「零 IV 的单块 AES-CBC」凑出原始块加密原语，再手工实现 CFB 反馈链，已与 OpenSSL 逐位比对一致。

WebVPN 域名下挂着全校站点，插件只在编码段确实对应 `cc98.org` 时才注入界面，不会在教务网等页面冒出悬浮按钮。

## 已知限制

- 内容过滤用「规则层 + 本地 BGE 模型」叠加（阈值可在设置页调）；模型跑在后台 SW，SW 空闲被回收后下次搜索需重新加载（几秒）。
- 后端 `feishu` 渠道在代码里声明了但没实现，前端只暴露钉钉和邮箱。
- 订阅配额上限后端没通过接口暴露，前端用兜底值 10。
- 插件没有图标资源，在扩展列表里显示为默认拼图。
